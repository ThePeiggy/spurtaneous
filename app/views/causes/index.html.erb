<% @causes.each do |cause| %>
	<div class="jumbotron">
		<p>Test Cause: <%= cause.name %></p>
		<%= link_to "Hit", add_hit_cause_path(cause), remote: true, class: 'btn btn-large btn-primary' %>
	</div>
<% end %>
